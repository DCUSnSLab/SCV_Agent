# FTA 진행현황

> 상시 갱신되는 운영 문서. 매 작업 세션 종료 시 갱신한다. 작업 정의는 [05_프로젝트계획서.md](05_프로젝트계획서.md) 참조.

- 최종 갱신: 2026-07-24

## 마일스톤 상태

| 마일스톤 | 상태 | 완료 기준 충족 | 비고 |
|---|---|---|---|
| M1 골격 + 최소 종단 연결 | ✅ 완료 | ✅ 충족 (2026-07-24) | 종단 검증: 103건 무손실, 평균 지연 0.49 ms |
| M2 샘플러 세트 + 다중 파이프라인 | ✅ 완료 | ✅ 충족 (2026-07-24) | 5개 파이프라인 동시 검증, 단위 테스트 34건 |
| M3 코덱 + Bulk 처리 | ✅ 완료 | ✅ 충족 (2026-07-24) | 실측 rosbag 재생 검증, [대역폭 리포트](reports/M3_bandwidth_report.md) |
| M4 신뢰성 | ✅ 완료 | ✅ 충족 (2026-07-24) | 5분 차단 공식 테스트 PASS — 이벤트 손실 0건 |
| M5 다운링크 (동적 인터페이스) | ✅ 완료 | ✅ 충족 (2026-07-24) | 자동화 12/12 PASS (거부 4종 포함) |
| M6 관측성 + 운영 준비 | ✅ 완료 | ✅ 충족 (2026-07-24) | [성능 리포트](reports/M6_performance_report.md), soak 스크립트 준비 |

## M1 작업 체크리스트 — 완료 (2026-07-24)

- [x] 1.1 colcon 패키지 골격 (`fta_agent`, `fta_tools`)
- [x] 1.2 범용 레지스트리 + ISampler/ICodec/ITransport ABC
- [x] 1.3 ConfigLoader (스키마 검증, `${ENV}` 치환)
- [x] 1.4 Envelope (CBOR, version 필드)
- [x] 1.5 파이프라인 (generic subscription → 큐 → 워커 → 우선순위큐)
- [x] 1.6 passthrough 샘플러 + cbor 코덱 + MqttTransport
- [x] 1.7 UplinkManager (단순 드레인)
- [x] 1.8 테스트 리시버 (jsonl 기록 + 통계)
- [x] 1.9 단위 테스트 (pytest 19건 통과)
- [x] **완료 기준 충족**: 가상 `/odom` 10 Hz 발행 → 리시버 jsonl 103건 기록, seq 0~102 결손 0, 평균 종단 지연 0.49 ms, 필드 값 무결(x=7.7) — 아래 검증 로그 참조

### M1 검증 증빙 (2026-07-24)

```
# agent 로그
파이프라인 'odom' 통계: {'in': 103, 'sampled_out': 0, 'encoded': 103, 'drop_in_full': 0, 'error': 0}
업링크 통계: {'published': 103, 'publish_failed': 0}, 연결: connected

# receiver 통계
r01: 50 msgs, 86.8 kbps, 평균 지연 0.5 ms

# jsonl 무결성 검사
수신 103건, seq 0~102, 결손 [], 평균지연 0.49ms, x=7.7
```

부수 확인: 동일 client_id 중복 접속 시 브로커가 기존 세션을 밀어내고 paho 자동 재접속이 동작함(핑퐁)을 관찰 — M4 재연결 설계 시 client_id 유일성 전제 반영.

## M2 작업 체크리스트 — 완료 (2026-07-24)

- [x] 2.1 `rate` 샘플러 (목표 Hz 다운샘플)
- [x] 2.2 `deadband` 샘플러 + 필드 경로 헬퍼 (`pose.pose.position.x`, `status[0].level` 형식)
- [x] 2.3 `event` 샘플러 (changed/eq/ne/gt/gte/lt/lte, 엣지 트리거, PASS_AND_FLUSH)
- [x] 2.4 `on_demand` 샘플러 (request() 스레드 안전 — 트리거 서비스는 M3)
- [x] 2.5 큐 정책: high 포화 시 파이프라인별 conflation, low/normal 오래된 것부터 드롭
- [x] 2.6 파이프라인별 QoS override 검증 (`transient_local` latched 구독 — M1 구현분 실증)
- [x] 2.7 다중 파이프라인 예시 설정 (`fta_m2_example.yaml`, 인벤토리 유형 반영)
- [x] **완료 기준 충족**: 인벤토리 유형별 파이프라인이 설정만으로 구성·동시 동작, 단위 테스트 34건 통과

### M2 검증 증빙 (2026-07-24)

5개 파이프라인 동시 동작, 12초 발행 시나리오:

```
파이프라인          수신   실효 Hz   평균 지연   결과
odom  (50Hz 발행)   22건   1.97 Hz   0.37 ms   rate 2Hz 다운샘플 ✓
imu   (50Hz 발행)   54건   4.74 Hz   0.93 ms   rate 5Hz 다운샘플 ✓
battery (5Hz 발행)   2건       —     0.35 ms   deadband — 0.8→0.9 변화 2건만 통과 ✓
vehicle_estop        2건       —     0.33 ms   event changed — 초기 false + 변화 true,
                                               fleet/r01/event/* 채널 ✓
planned_path         1건       —     0.25 ms   에이전트 기동 전 latched 발행분을
                                               transient_local 구독으로 수신 ✓
전체 seq 결손 0
```

부수 수정: SIGTERM 종료 시 ExternalShutdownException 트레이스백 제거 (정상 종료 경로 처리).

## M3 작업 체크리스트 — 완료 (2026-07-24)

- [x] 3.1 `cdr_zstd` 코덱 (typesupport 불필요 — CDR 원본 + zstd)
- [x] 3.2 `jpeg` 코덱 (quality/max_width, Image·CompressedImage 지원)
- [x] 3.3 `voxel_zstd` 코덱 (복셀 다운샘플 + zstd, 혼합 dtype 클라우드 대응)
- [x] 3.4 로컬 스냅샷 서비스 `/fta/request_snapshot/{pipeline}` (std_srvs/Trigger, 덕 타이핑 연결)
- [x] 3.5 대역폭 측정 리포트 → [reports/M3_bandwidth_report.md](reports/M3_bandwidth_report.md)
- [x] **완료 기준 충족**: 실측 rosbag 재생으로 카메라·LiDAR 온디맨드 스냅샷 종단 동작
  (JPEG 73KB 1건, voxel 54KB/4,872점 1건 수신·파일 저장 확인), 단위 테스트 40건 통과

### M3 검증 증빙: 상태 6.6kbps, 카메라 프리뷰 122.8kbps(원본 대비 2.2%), LiDAR 34kbps(1.6%) — 리포트 참조

## M4 작업 체크리스트 — 완료 (2026-07-24)

- [x] 4.1 DiskBuffer (append-only 세그먼트, 상태=최신값 유지, 디스크 상한, 재시작 복구, 잘린 레코드 내성)
- [x] 4.2 재연결 지수 백오프 (paho 1→30s) + 재연결 감지 시 백로그 우선 재전송
- [x] 4.3 우선순위별 드롭/conflation + 단절 라우팅 (이벤트→전량, 상태→최신값, bulk→폐기)
- [x] 4.4 토큰버킷 대역폭 상한 (critical 우회, NFR-2.5)
- [x] 4.5 단절 자동화 테스트 `tests/integration/m4_disconnect_test.sh`
- [x] **완료 기준 충족 (NFR-3.1)**: 브로커 강제 차단 5분 → 복구 시 이벤트 손실 0건 — 아래 증빙

### M4 검증 증빙 (2026-07-24) — 5분 차단 공식 실행 PASS

```
이벤트 수신: 고유 701건 (0~700), 결손 없음, 중복 0건
단절 구간 발생 이벤트: 602건 — DiskBuffer 보존 → 복구 후 전량 전달 (NFR-3.1)
복구→첫 수신: 1.4s (재연결 백오프 포함, NFR-3.2 ≤10s)
에이전트 로그: 재접속 0.15초 내 "DiskBuffer 재전송: 603건 (완료=True)"
```

- 25s 스모크: 재연결 5.6s, 단절 중 52건 무손실 (백오프 위상에 따라 재연결 1~30s 변동)
- 테스트 하네스 개선 이력 (에이전트 결함 아님): ① 리시버 paho 기본 백오프(최대 120s)로
  관측 지연 → 영속 세션+빠른 재접속으로 수정, ② 브로커 재시작 시 세션 소실로 drain분
  유실 관측 → 테스트 브로커 persistence 활성화. **→ 실서버 브로커 요구사항(persistence +
  인제스천 내구 세션)으로 서버 프로젝트 계약에 반영 필요 (config_reference.md에 명시)**

## M5 작업 체크리스트 — 완료 (2026-07-24)

- [x] 5.1 RegistrySyncManager — retained 동기화, 항목 검증, typesupport 확인, registry_status 보고
- [x] 5.2 JsonMessageConverter — rosidl introspection JSON↔msg (D7: 서버는 타입 무지)
- [x] 5.3 CommandExecutor — 검증 체인 순서 고정(등록→스키마→TTL→멱등성, NFR-7.5), topic 발행/service 호출, cmd_result 응답
- [x] 5.4 감사 로그 (NFR-7.6, jsonl)
- [x] 5.5 테스트 레지스트리 도구 `registry_tool` (파일→retained 발행, 발행 포맷=서버 계약)
- [x] 5.6 거부 4종 자동화 테스트 (TTL 만료/중복 cmd_id/미등록/스키마 위반)
- [x] **완료 기준 충족**: (a) 레지스트리 등록만으로 set_goal 종단 동작(/goal_pose 수신 확인),
  (b) 거부 4종 + ttl>default_ttl 거부 + service kind(스냅샷) 자동화 12/12 PASS

### M5 검증 증빙 (2026-07-24)

`tests/integration/m5_downlink_test.sh` — PASS 12 / FAIL 0:
accepted·expired·duplicate(1회만 실행)·미등록 rejected·스키마 rejected·ttl 초과 rejected·
service done(success=true)·/goal_pose 발행(x=3.5)·스냅샷 업링크 수신·
hunter_msgs 미지원 registry_status 보고(음성 케이스)·감사 로그 14건.
단위 테스트: 검증 체인 8케이스 + 레지스트리 동기화 3케이스 포함 총 60건 통과.

## M6 작업 체크리스트 — 완료 (2026-07-24)

- [x] 6.1 SelfTelemetry — `fleet/{id}/agent/health` + 로컬 `/fta/health` (FR-7.1/7.2)
- [x] 6.2 ResourceGovernor — CPU/RSS 감시, 임계 초과 시 bulk 일시정지(히스테리시스)
- [x] 6.3 systemd 유닛 (`deploy/fta_agent.service`: CPUQuota 20%/MemoryMax 512M/Restart, `fta.env.example`)
- [x] 6.4 구조화 로깅 (`--log-format json`)
- [x] 6.5 rosbag 재생 성능 테스트 → [reports/M6_performance_report.md](reports/M6_performance_report.md)
- [x] 6.6 72h soak 스크립트 `tools/soak_test.sh` (축약 실행으로 스크립트 검증)
- [x] 6.7 문서: [config_reference.md](config_reference.md), [extending.md](extending.md)
- [x] **완료 기준 충족**: soak 스크립트 준비 + 설정 레퍼런스·확장 가이드 문서

### M6 검증 증빙 (2026-07-24)

- NFR-1: CPU 최대 12% (상한 20%), RSS 최대 136MB (상한 512MB) — v1 최대 부하(카메라 30Hz 수신+JPEG+복셀)
- NFR-2: 업링크 175kbps (목표 100~300), 지연 p95 <2ms(로컬), 스냅샷 0.74s (목표 3s)
- `/fta/health` 로컬 토픽·JSON 로그 실기동 확인

## 결정 로그

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-07-24 | **D1 언어: Python 3.10 / rclpy 전체 구현 확정** | 팀 결정. 성능 리스크는 M3/M6에서 측정 후 병목만 선택 최적화 (05 문서 §5) |
| 2026-07-24 | **D5 배포판: ROS2 Humble / Ubuntu 22.04 확정** | 개발 머신 환경 실사 일치 |
| 2026-07-24 | 개발 브로커: 로컬 mosquitto 평문(1883). TLS는 M4 이후 | 킥오프 체크리스트 "개발용은 로컬 mosquitto" |
| 2026-07-24 | docs 파일명 유니코드 NFC 정규화 | 기존 NFD(macOS 생성) 파일명이 Linux/GitHub 링크와 불일치 |
| 2026-07-24 | 개발 브로커를 docker eclipse-mosquitto:2로 기동 | 개발 머신 sudo 제약. 설정: `.dev/mosquitto/mosquitto.conf`. 네이티브 mosquitto 설치와 동등 |
| 2026-07-24 | M1 cbor 코덱 = introspection 기반 msg→dict→CBOR | 수신측 무의존 디코딩으로 M1 검증 용이. CDR 원본+zstd(cdr_zstd)는 계획대로 M3에서 |
| 2026-07-29 | **발견 이슈는 즉시 개별 수정하지 않고 본 문서 "이슈 / 개선 백로그" A그룹에 모아 일괄 처리** | 문서화·검증 작업 중 발견되는 소규모 결함이 여러 커밋에 흩어지는 것을 방지. 수정 세션에서 한 번에 처리 후 통합 테스트 재실행 |

## 이슈 / 개선 백로그

> **운영 규칙**: 발견되는 문제는 개별로 즉시 고치지 않고 **이 표 한 곳에만 모은다.**
> 모인 항목은 별도 수정 세션에서 **일괄 처리**한다 (결정 D-2026-07-29).
> 마지막 수집: 2026-07-29 · 미처리 A그룹 9건 (업링크 관련: A-3·A-7·A-8·A-9)

### A. 수정 대상 (일괄 처리 대기)

| # | 대상 | 증상 / 재현 | 영향 | 제안 수정 | 상태 |
|---|---|---|---|---|---|
| A-1 | `tests/integration/m5_downlink_test.sh:74` | [5]단계 `/goal_pose` 확인이 관찰자 `ros2 topic echo`에 의존. 2026-07-29 ros2cli 데몬 XML-RPC 오류로 echo가 기동 실패 → 오탐 FAIL 1건 (감사 로그에는 `/goal_pose 1회 발행` 기록되어 기능은 정상) | 테스트 신뢰도 (기능 영향 없음) | 관찰자를 rclpy 구독 스크립트로 교체 (데몬 비의존) | 열림 |
| A-2 | `tests/integration/m5_downlink_test.sh:74,78,82,86` | `X=$(grep -c ... \|\| echo 0)` — 매치 0건이면 grep이 "0" 출력 + exit 1 → `echo 0`이 덧붙어 `"0\n0"`. 이후 `[ "$X" -ge 1 ]`이 `[: integer expression expected`로 실패 | 실패 시 에러 메시지 오염 | `X=$(grep -c ... ; true)` 또는 `X=$(grep -c ... \|\| true); X=${X:-0}` 형태로 정리 | 열림 |
| A-3 | `fta_agent/fta_agent/samplers/event.py` | `octet`/`uint8` 필드(예: `DiagnosticStatus.level`)를 rclpy가 `bytes`로 노출 → `condition: gte, value: 1` 같은 수치 비교가 `TypeError: '>=' not supported between 'bytes' and 'int'`. 파이프라인은 메시지만 폐기하고 계속 동작(설계대로)하나 설정자가 원인을 알기 어려움 | 사용성 (설정 함정) | 비교 전 `bytes`(길이 1) → `int` 정규화, 또는 기동 시 필드 타입 검사로 조기 경고 | 열림 |
| A-4 | `fta_tools/fta_tools/test_receiver.py:103` | 비-envelope 채널 필터가 `/sys/`·`/agent/`·`/cmd`만 제외. retained JSON인 `fleet/registry`는 통과되어 CBOR 디코딩 시도 → `디코딩 실패 (fleet/registry)` 로그 + `decode_error` 카운터 증가 (재접속마다 반복) | 로그 노이즈, 통계 오염 | 필터에 `fleet/registry` / `/registry` 추가 | 열림 |
| A-5 | 통합 테스트 스크립트 전반 (`m4/m5/m6`, `tools/soak_test.sh`) | `kill $PID`가 `ros2 run` 래퍼만 종료하고 자식 파이썬이 잔존. 잔존 에이전트가 동일 `client_id`로 재접속해 다음 실행을 계속 킥 → 2026-07-29 m5 1차 실행 11건 오탐 실패 | 테스트 재현성 | 스크립트를 `setsid`로 기동하고 `kill -- -$PGID`로 그룹 종료, 종료 후 잔존 확인 | 열림 |
| A-6 | `CLAUDE.md:61` | `/usr/bin/python3 -m pytest fta_agent/test/ -v`를 저장소 루트에서 그대로 실행하면 `ModuleNotFoundError: No module named 'fta_agent.codecs'` (루트의 `fta_agent/` 디렉토리가 먼저 잡힘). `source install/setup.bash` 선행 시 60건 통과 | 문서 정합성 | 명령에 `source install/setup.bash` 선행 표기 | 열림 |
| **A-7** | `core/priority_queue.py:31-43` (업링크) | 큐 포화 시 `critical`도 오래된 항목을 드롭하고 `dropped.critical`로 집계만 함. 코드 docstring의 설계 의도는 "critical 드롭 금지 → DiskBuffer 이관(M4)"이나, M4에서 DiskBuffer는 **단절·발행실패·토큰 타임아웃 경로에만** 연결됨. 즉 연결이 살아있는데 인코딩 속도 > 전송 속도로 큐가 포화되면 critical/event가 디스크로 가지 않고 유실 | **데이터 유실 가능 (FR-5 취지 위반)** — 단절 테스트로는 검출되지 않는 경로 | 큐 포화 시 critical(및 `msg_class: event`)을 드롭 대신 `UplinkManager._route_offline`으로 넘겨 DiskBuffer에 적재 | 열림 |
| **A-8** | `core/uplink_manager.py:62-67` (업링크) | DiskBuffer `drain()`이 **재연결 엣지**(`connected and not self._was_connected`)에서만 호출됨. ① 연결 유지 상태에서 토큰버킷 30초 타임아웃으로 `_route_offline`된 데이터, ② drain 중간 실패로 남은 세그먼트는 다음 재연결이 일어날 때까지 재전송되지 않음 | 대역폭 상한 상시 초과 환경에서 이벤트 전달 지연이 무한정이 될 수 있음 | 루프에서 `buffer.pending()`이 비어있지 않으면 연결 상태에서 주기적으로 drain 재시도 | 열림 |
| A-9 | `transports/mqtt_transport.py:53-54` | `tls: true`가 `tls_set()` 무인자 호출 = 시스템 CA 신뢰저장소만 사용. 사설 CA 지정(`ca_certs`)·클라이언트 인증서(mTLS) 경로가 없음. NFR-5.2는 "client credential **또는 인증서**"를 허용하고 03 문서는 서버측 인증서 발급/폐기 운영을 전제 | 서버가 인증서 기반 인증을 택하면 운영 배포 불가 | 설정에 `mqtt.ca_certs`/`certfile`/`keyfile`(경로는 ENV 주입) 추가 후 `tls_set`에 전달 | 열림 |

### B. 환경 제약 (수정 대상 아님 — 상시 주의)

| # | 이슈 | 대응 |
|---|---|---|
| B-1 | `hunter_msgs` typesupport 개발 머신 미설치 | 업링크(CDR passthrough)는 무관. M5 음성 테스트 케이스로 활용 중. 실 로봇 배포 전 msg 패키지 확보 필요 |
| B-2 | 개발 머신 기본 Python이 pyenv 3.13 — rclpy는 시스템 3.10 필요 | 빌드·실행·pip 모두 `/usr/bin/python3` 기준 (CLAUDE.md 명시) |

## 세션 로그

### 2026-07-29 — README 사용자 문서화 (설치·운영·무서버 테스트)
- README 전면 개편: ① 서버 없이 5분 빠른 시작(테스트 리시버 + `ros2 topic pub` 가짜 발행자),
  ② 무서버 다운링크 시험(레지스트리 발행 → 명령 → 거부 5종 표), ③ 실제 로봇 설치·운영
  (/opt/fta 레이아웃, 배포 빌드, 설정 작성 가이드, 프로비저닝/TLS, systemd, 설치 후 점검,
  업데이트·제거), ④ 운영 관측 지점, ⑤ 트러블슈팅 12항목
- 기재 절차는 전부 실측 검증: 종단 47건 수신(지연 0.4ms)·단위 60건·colcon test 60건·
  m5 다운링크(감사 로그 14건, 이슈 #3 오탐 1건 제외 전 항목 PASS)·배포 레이아웃 빌드/기동·
  기동 실패 메시지(ROBOT_ID 미정의/typesupport 없음)·브로커 부재 시 무크래시 재접속·
  `/fta/health` echo·json 로그 포맷
- 배포 지침 확정: 운영 빌드에 `--symlink-install` 금지(소스 트리가 런타임 의존이 됨).
  스크래치에서 `colcon build`로 재현 — install 트리에 소스 심볼릭 링크 0건 확인
- 트러블슈팅에 반영한 실제 함정: `ros2 run`을 kill하면 자식 파이썬이 잔존 → 동일
  client_id로 재접속하여 후속 에이전트를 계속 킥(이번 세션 m5 1차 실행 11건 실패 원인)
- (추가) README 5장 "업링크 설정 작성 가이드" 신설: 3블록 구조·파이프라인 키별 의미·
  sampler/codec/priority/msg_class 선택 기준표·데이터 유형 12종 예시·작성 절차·자주 하는 실수.
  12개 파이프라인 설정과 합성 발행자(9개 토픽)를 만들어 20초 실행 검증 — 전 파이프라인 error 0,
  rate 50→2Hz·100→5Hz, deadband 20→7·20→2, event 엣지 20→2~5, latched 1건, jpeg/voxel/on_demand
  스냅샷(서비스 트리거) 정상. 결과 수치를 README 5.5 표에 그대로 기재
- 발견: `DiagnosticStatus.level` 등 `octet` 필드는 rclpy가 `bytes`로 노출 → event 샘플러의
  수치 비교(gte 등)에서 TypeError. 파이프라인은 해당 메시지만 폐기하고 계속 동작(설계대로).
  `changed` 사용 또는 수치 필드 선택으로 회피 — README 5.7·8장에 명시
- 업링크 코드 재점검(PR 머지 전): A-7(큐 포화 시 critical 드롭 — DiskBuffer 미연결),
  A-8(drain이 재연결 엣지에서만 호출 — 상한 초과 시 배출 지연), A-9(TLS 사설 CA/mTLS 경로 없음) 추가.
  A-7·A-8은 단절 테스트(M4)로는 검출되지 않는 경로 — 일괄 수정 시 재현 테스트 동반 필요
- 이슈 관리 방식 변경: 이번 세션에서 발견한 결함 6건(A-1~A-6)을 개별 수정하지 않고
  "이슈 / 개선 백로그" A그룹 한 곳에 수집. 다음 수정 세션에서 일괄 처리 후 통합 테스트 재실행
  (m5 관찰자·`grep -c` 패턴, event 샘플러 octet 비교, 리시버 registry 디코딩 노이즈,
  테스트 스크립트 프로세스 그룹 종료, CLAUDE.md pytest 명령)

### 2026-07-24 (3차) — M3~M6 완료, v1 마일스톤 전체 종료
- M3: 코덱 3종 + 스냅샷 서비스 + 실측 rosbag 대역폭 리포트 (voxel 코덱 혼합 dtype 버그 수정)
- M4: DiskBuffer/토큰버킷/단절 라우팅 — 5분 차단 공식 테스트 PASS (이벤트 701건 결손 0)
- M5: 다운링크 검증 체인(NFR-7)·레지스트리 동기화·감사 로그 — 자동화 12/12 PASS
- M6: SelfTelemetry/ResourceGovernor/systemd/구조화 로깅/성능 리포트(NFR-1·2 충족)/soak 스크립트
- 단위 테스트 60건, 통합 테스트 스크립트 3종(tests/integration/), 브로커: 네이티브 mosquitto 전환
- 남은 항목: 72h soak 실제 실행(스크립트 준비 완료), 실 로봇·LTE 파일럿 검증,
  TLS 운영 설정, 서버 프로젝트 계약 전달(브로커 persistence 요구사항 포함)

### 2026-07-24 (2차) — M2 완료
- 샘플러 4종(rate/deadband/event/on_demand) + 필드 경로 헬퍼 구현
- 우선순위 큐 conflation 정책(high), 다중 파이프라인 예시 설정
- 단위 테스트 34건, 5개 파이프라인 종단 검증 통과 (위 증빙)
- 브로커 전환: docker → 네이티브 mosquitto (사용자 설치). 현재 1883 포트 점유 문제로
  사용자 프로세스로 기동 중 (localhost 전용) — 재부팅 시 systemd 서비스가 자동 인계,
  또는 `sudo systemctl restart mosquitto`로 즉시 전환 가능
- 다음: M3 (cdr_zstd/jpeg/voxel_zstd 코덱, 스냅샷 서비스, 대역폭 측정 리포트)

### 2026-07-24 — 프로젝트 킥오프 + M1 완료
- 문서 세트 작성: README.md, 05_프로젝트계획서(WBS), 06_진행현황(본 문서), CLAUDE.md
- 01 문서 Decision Log 갱신 (D1/D5 확정)
- 데이터 인벤토리 실측 갱신 (rosbag2_2026_01_21-18_41_02 기준, 05 문서 §2)
- 개발 환경 셋업: docker mosquitto 브로커, python3.10 의존 패키지
- **M1 구현 완료**: fta_agent/fta_tools 패키지, 단위 테스트 19건, 종단 검증 통과 (위 증빙)
- 다음: M2 착수 전 구현 계획 승인 (rate/deadband/event/on_demand 샘플러 + 다중 파이프라인)
